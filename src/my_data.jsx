import React, { useState, useRef, useEffect, useMemo } from 'react';
import CustomButton from './components/Button';

const MyData = ({ saveChanges, setSaveChanges }) => {
  return <div></div>;
};

export default MyData;
